version: 2

models:
  - name: stg_dim_club_memmber_status
    columns: 
      - name: customer_id
        tests:
          - unique
          - not_null

  - name: stg_dim_costumer_age
    columns:
      - name: customer_id
        tests:
          - unique
          - not_null


      